using RPGClassLibrary.Actors;
using RPGClassLibrary.Operations;

namespace RPGClassLibrary.Combat
{
    public class CombatHandler
    {
        public static void BattleStarter(Player p)
        {
            //create enemy(Enemy.EnemyFactory())
            //pass current player and enemy into battle method (CombatHandler.BattleLogic(Player, enemy)
        }

        public static void BattleLogic(Enemy e, Player p)
        {
            
        }
    }
}